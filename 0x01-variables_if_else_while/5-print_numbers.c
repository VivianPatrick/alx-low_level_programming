#include <stdio.h>


/**
 * main - Entry point
 * Descriptio: Program that prints all single digit numbers of base 10 from 0
 * Return: Always 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	putchar('\n');
	return (0);
}

